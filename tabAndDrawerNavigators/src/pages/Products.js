import React from 'react';
import { View, Text } from 'react-native';

function Products() {
    return(
        <View style={{flex: 1, justifyContent: 'center',alignItems: 'center' }}>
            <Text style={{ fontSize: 40, }}>Hello Products</Text>
        </View>
    )
}

export default Products;